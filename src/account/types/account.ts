/**
 * TODO: sex возвращается как boolean
 * Исправить на бэке
 */

export type Profile = {
    id: number;
    date_of_birth: string;
    email: string;
    phone: string;
    first_name: string;
    last_name: string;
    second_name: string;
    sex: boolean;
    subscribe: boolean;
    email_verified_at: boolean;
    phone_verified_at: boolean;
};
